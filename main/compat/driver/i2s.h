#pragma once
// Compatibility stub for ESP-IDF v5.x where legacy driver/i2s.h is removed.
// The MicroPython config includes this header but the port does not use it
// when I2S support is disabled in our build.
